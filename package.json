{
  "name": "allium-assault",
  "version": "1.26.2",
  "description": "Turn-based 3D artillery game with garlic buddies, in the spirit of Worms Armageddon. Runs in the browser.",
  "private": true,
  "type": "module",
  "author": "Marcel Petrick <mail@marcelpetrick.it>",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173 --strictPort",
    "lint": "eslint . && prettier --check . && stylelint \"src/**/*.css\" && markdownlint-cli2 \"**/*.md\" && npm run lint:spdx",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "e2e": "playwright test",
    "verify": "npm run lint && npm run typecheck && npm test && npm run build && npm run e2e",
    "lint:spdx": "node scripts/spdx.mjs",
    "spdx:fix": "node scripts/spdx.mjs --fix",
    "lint:reuse": "uvx reuse lint",
    "format": "prettier --write . && stylelint \"src/**/*.css\" --fix && markdownlint-cli2 --fix \"**/*.md\"",
    "capture-media": "node scripts/capture-media.mjs"
  },
  "dependencies": {
    "@babylonjs/core": "9.26.2",
    "simplex-noise": "4.0.3"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@playwright/test": "1.63.0",
    "@types/node": "24.13.5",
    "eslint": "10.10.0",
    "globals": "17.12.0",
    "markdownlint-cli2": "0.23.2",
    "prettier": "3.9.7",
    "stylelint": "17.15.0",
    "stylelint-config-standard": "40.0.0",
    "typescript": "6.0.3",
    "typescript-eslint": "8.70.0",
    "vite": "8.3.0",
    "vitest": "5.0.1"
  },
  "overrides": {
    "smol-toml": "1.8.0"
  }
}
